name: endorse
homepage: "https://github.com/kirklink/endorse"
description: "AI-first validation library for Dart with code generation."
version: 2.0.0-dev.1

environment:
  sdk: ^3.0.0

dependencies:
  analyzer: ^10.1.0
  build: ^4.0.0
  source_gen: ^4.2.0

dev_dependencies:
  build_runner: ^2.11.0
  lints: ^5.0.0
  test: ^1.25.0
